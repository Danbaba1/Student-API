/** @type {import('jest').Config} */
const config = {
    // Specifies the test environment for Node.js
    testEnvironment: 'node',

    // Disables default CommonJS code transforms so files emit natively as ESM
    transform: {},
};

export default config;
